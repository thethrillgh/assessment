<!DOCTYPE html>
<html>
<head>
	<title><%=title %></title>
</head>
<body>
	<% if (loggedIn) { %>
		Welcome back, <%=department %>
		<br>
		<a href="logout">Logout</a>
	<% } else { %>
		<a href="signin">Login</a>
	<% } %>
</body>
</html>